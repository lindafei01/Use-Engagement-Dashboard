# Optional: full backend URL when not using Vite dev proxy (e.g. production build behind nginx).
# Leave unset during local dev — requests go to same origin and Vite proxies /api → backend.
# VITE_API_BASE=http://127.0.0.1:8765
